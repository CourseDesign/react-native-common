import { ImageStyle, TextStyle, ViewStyle } from 'react-native';

/**
 * `React Native`에서 사용되는 스타일을 나타내는 타입입니다.
 */
type ReactNativeStyle = TextStyle | ImageStyle | ViewStyle;

export default ReactNativeStyle;
