import { ImageProps, TextProps, ViewProps } from 'react-native';

/**
 * `React Native`에서 사용되는 `props`을 나타내는 타입입니다.
 */
type ReactNativeProps = TextProps | ImageProps | ViewProps;

export default ReactNativeProps;
