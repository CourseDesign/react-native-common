import ProgressProps from './ProgressProps';

/**
 * `ProgressCircle`컴포넌트의 `Props` 타입입니다.
 */
interface ProgressCircleProps extends ProgressProps {
  /**
   * `ProgressBar`의 색상을 결정합니다.
   */
  color?: string;
}

export default ProgressCircleProps;
